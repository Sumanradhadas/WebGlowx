I want to modify my current website’s contact form feature.
Currently, it stores submitted messages in Replit’s local database — I want to remove that database entirely.

Instead, when someone submits the contact form, it should directly send an email notification to my Yahoo email using Nodemailer and environment variables for security.

Here’s what I need done:

Remove all database code

Delete all references to Replit DB, Database(), db.set(), db.get(), etc.

No data should be stored locally or in any DB.

Install dependencies

npm install express nodemailer body-parser cors dotenv


Add .env file with these variables (replace with real info):

MY_EMAIL=youraddress@yahoo.com
MY_EMAIL_PASS=your_yahoo_app_password


This uses Yahoo Mail, so the app password is required (not the normal Yahoo password).

Generate it from: https://login.yahoo.com/account/security
 → “App Passwords”.

Update the backend (server.js or index.js)
Replace the old contact form endpoint with this new one:

import express from "express";
import bodyParser from "body-parser";
import cors from "cors";
import nodemailer from "nodemailer";
import dotenv from "dotenv";

dotenv.config();
const app = express();
const PORT = process.env.PORT || 5000;

app.use(cors());
app.use(bodyParser.json());

app.post("/contact", async (req, res) => {
  const { name, email, message } = req.body;

  if (!name || !email || !message) {
    return res.status(400).json({ error: "All fields are required" });
  }

  try {
    const transporter = nodemailer.createTransport({
      service: "yahoo",
      auth: {
        user: process.env.MY_EMAIL,
        pass: process.env.MY_EMAIL_PASS,
      },
    });

    await transporter.sendMail({
      from: process.env.MY_EMAIL,
      replyTo: email,
      to: process.env.MY_EMAIL,
      subject: `New Contact Message from ${name}`,
      text: `From: ${name}\nEmail: ${email}\n\n${message}`,
    });

    res.status(200).json({ success: "Message sent successfully!" });
  } catch (error) {
    console.error("Email send failed:", error);
    res.status(500).json({ error: "Failed to send email" });
  }
});

app.listen(PORT, () => console.log(`Server running on port ${PORT}`));


Keep the frontend form as it is, but make sure the JavaScript sends the form data to /contact using fetch:

document.getElementById("contactForm").addEventListener("submit", async (e) => {
  e.preventDefault();
  const formData = {
    name: e.target.name.value,
    email: e.target.email.value,
    message: e.target.message.value,
  };

  const res = await fetch("/contact", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(formData),
  });

  const data = await res.json();
  alert(data.success || data.error);
});


Test by submitting the contact form.
You should receive an email instantly at your Yahoo inbox.

Do not store anything in any database.
This system should purely email each submission directly.